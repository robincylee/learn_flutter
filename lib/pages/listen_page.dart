import 'package:flutter/material.dart';

class ListenPage extends StatelessWidget {
  String listenTitle = "Test";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('$listenTitle'),
      ),
      body: Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
